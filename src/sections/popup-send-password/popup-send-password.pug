mixin popup-send-password(section)
	//- проверка данных получаемых из section
	include ../../shared/helpers/pug/shortcut-check-store

	//- верстка секции
	+Popup(S_ELEM('Popup'))
		+e('title', {}, 'div') #{S_DATA('title')}
		+e('line', {}, 'div')
		+Text(S_ELEM('TextSendPassword'))
		+Text(S_ELEM('TextEmail'))
		+e('button-password', {m: 'none'}, 'button') #{S_DATA('buttonClose')}
		+Text(S_ELEM('TextReservePersonalData'))
		+Text(S_ELEM('TextReservePersonalText'))
