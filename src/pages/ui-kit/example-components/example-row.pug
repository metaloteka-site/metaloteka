style.
	.example-row div:not(.row) {
		--wet-asphalt: #46464692;
		background: var(--wet-asphalt);
		padding-inline: 10px;
	}

+b('h3', { b: 'mt-20' }, 'h3') Компонент Row
+b('p', {}, 'p') - Данный компонент вызывается как #[code +Row].
+b('p', {}, 'p') - Данный компонент имеет заранее определенные стили. При переполнении он переносит контент внутри на новую строчку.
+b('p', {}, 'p') - Принимает следующие параметры #[code rowCols], #[code gap], #[code justify], #[code alignItems].
+b('example-row', { b: 'mt-10' }, 'div')
	+b('h4', {}, 'h4') Использования компонента без параметров
	+Row
		div Пример использования компонента без параметров
		div Пример использования компонента без параметров
		div Пример использования компонента без параметров
		div Пример использования компонента без параметров
		div Пример использования компонента без параметров
	+b('h4', { b: 'mt-10' }, 'h4') Использования компонента с параметром - rowCols
	+Row({ bem: { rowCols: 'row-cols-3' } })
		div Пример использования компонента с rowCols
		div Пример использования компонента с rowCols
		div Пример использования компонента с rowCols
		div Пример использования компонента с rowCols
		div Пример использования компонента с rowCols
	+b('h4', { b: 'mt-10' }, 'h4') Использования компонента с параметром - gap
	+Row({ bem: { gap: 'cg-20 rg-10' } })
		div Пример использования компонента с gap
		div Пример использования компонента с gap
		div Пример использования компонента с gap
		div Пример использования компонента с gap
		div Пример использования компонента с gap
	+b('h4', { b: 'mt-10' }, 'h4') Использования компонента с параметром - rowCols и gap
	+Row({ bem: { rowCols: 'row-cols-3', gap: 'cg-20 rg-10' } })
		div Пример использования компонента с rowCols и gap
		div Пример использования компонента с rowCols и gap
		div Пример использования компонента с rowCols и gap
		div Пример использования компонента с rowCols и gap
		div Пример использования компонента с rowCols и gap
